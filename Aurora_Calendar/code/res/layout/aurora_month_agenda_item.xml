<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp">

    <TextView 
        android:id="@+id/month_agenda_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="12dp"
        android:maxWidth="270dp"
        android:drawableStart="@drawable/aurora_agenda_icon_blue"
        android:drawablePadding="15dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:textColor="@color/aurora_title_text_color"
        android:textSize="14sp" />

    <ImageView
        android:id="@+id/month_agenda_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/month_agenda_title"
        android:layout_marginLeft="2dp"
        android:layout_marginTop="15dp"
        android:src="@drawable/aurora_email_icon"
        android:clickable="false"
        android:visibility="gone" />

    <TextView
        android:id="@+id/month_agenda_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="40dp"
        android:layout_below="@id/month_agenda_title"
        android:layout_marginTop="1dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:textColor="@color/aurora_summary_text_color"
        android:textSize="12sp" />

    <LinearLayout
        android:id="@+id/month_agenda_right"
        android:layout_width="54dp"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:layout_marginRight="16dp"
        android:layout_centerVertical="true"
        android:background="?com.aurora:attr/actionBarLeftWidgetBackground"
        android:gravity="center" >

        <ImageView
            android:id="@+id/month_agenda_reminder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_alignParentBottom="true"
        android:background="@color/horizontal_divider" />
</RelativeLayout>